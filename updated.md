# 漏抓擊殺修復與備援機制 (Fallback) 更新總結

本次更新針對「少抓擊殺」的問題進行了全面的根因修復，並實作了基於 ID 記憶的備援系統（Fallback），確保不論發生什麼狀況都能精準捕捉每一筆擊殺。

## 1. 實作「ID 記憶與 Fallback (備援)」機制 (你提議的解法！)

正如你所建議的「為什麼不能記住有哪些 kill 的 id 呢」，我們現在把這個概念完美融入了系統中：

* **持久化記憶陣列**：
  在 `state.json` 的 `app_state` 內新增了三個記憶陣列：`matchKillIds`、`matchDeathIds`、`matchNukeIds`。
  現在每當主迴圈抓到你的擊殺時，不僅會 `totalKills + 1`，還會把該次擊殺的專屬 ID 永久存入記憶陣列。這保證了「同一個 ID 絕對不會被計算兩次」。

* **背景定期漏抓掃描 (Fallback Check)**：
  原本的程式會在沒有新擊殺的短暫閒置期間（約 0.25 秒），進行一次 `lastDmg=0` 的探測，這會一次拉出整場對戰目前為止的**完整歷史紀錄**。
  我新增了 `fallback_recover_missed_events` 函式，它會在每次探測時掃描這份完整的歷史紀錄：
  1. 它會找出這場遊戲中所有屬於你的擊殺。
  2. 接著比對 `matchKillIds` 陣列。
  3. **如果發現有你的擊殺 ID 不在陣列中，代表主程式剛剛漏抓了！**
  4. 備援系統會立刻將這筆漏網之魚補算進 `totalKills`，並補上記錄，同時記錄到 Debug Log。

* **中途開啟程式也能完美還原**：
  這套機制的巨大優勢在於：如果你打到一半才打開 Counter，或者 Counter 途中當掉重開。主程式原本會把歷史紀錄當作 `baseline_seen` 全部忽略（為了避免重複計算）。但現在 Fallback 機制會去檢查你的記憶陣列，發現這些擊殺都還沒算過，就會自動把它們全部補回來！

* **換場自動重置**：
  當偵測到 War Thunder 進入新的一局（ID 歸零重算）時，程式會自動清空這些 ID 記憶陣列，避免與上一場的 ID 發生撞號。

## 2. 修復 Windows 卡頓導致的「假性遊戲關閉」

* **問題描述**：原本的程式在 6/2 的更新中，只要遇到一次 `ConnectionRefusedError` 就會認定遊戲關閉，進而觸發重置。但在 Windows 環境下，如果 War Thunder 瞬間卡頓，導致 8111 Port 的連線佇列 (backlog) 塞滿，也會短暫噴出這個錯誤。這會導致遊戲明明還在進行中，追蹤器卻自行重置，把卡頓期間的所有擊殺直接當成「已看過」而吞掉。
* **修復方式**：我加入了 `refused_count` 變數。現在必須像 Timeout 一樣，**連續**遇到拒絕連線超過門檻（預設約 2 秒），才會真正判定遊戲關閉。這根除了卡頓導致的擊殺漏抓。

## 3. 修復「換場時前段擊殺錯過」的問題

* **問題描述**：換場偵測原本依賴 `新ID + 40 < 舊游標`。但如果上一場遊戲很早結束，舊游標很小（例如 30），那 `5 + 40 < 30` 永遠不會成立。這會導致追蹤器死當在舊游標，直接錯過新一局最前面的所有事件。
* **修復方式**：更改了邏輯，現在只要發現 `新ID < 舊游標`，就絕對是時光倒流（換場），確保每場遊戲都能精準從 0 開始抓取。

## 4. 修復「Fallback 備援機制從未觸發」的核心 Bug (6/3)

* **問題描述**：先前設計的 Fallback 備援機制，依賴 `empty_poll_count`（當沒有抓到新訊息時的計數器）達到門檻來啟動。但因為為了防止漏接封包，我們每次抓取都會「往回退 40 個 ID (`DMG_REFETCH_MARGIN`)」，導致 API 每次都會回傳最後 40 筆紀錄。也就是說，陣列「永遠不會是空的」，造成 `empty_poll_count` 永遠卡在 0，Fallback 備援機制在遊戲進行中**從來沒有被觸發過**，導致漏掉的擊殺永遠找不回來！
* **修復方式**：
  1. 將 `empty_poll_count` 的判斷條件從「陣列是否為空」改為「**是否有抓到任何『新』的 ID**」。只要一段時間沒有產生新擊殺，備援機制就會正確啟動。
  2. 為了避免背景掃描太過頻繁（原設定為 0.25 秒掃描整場對戰）導致遊戲卡頓，將觸發門檻 `EMPTY_CURSOR_PROBE_THRESHOLD` 延長至 100（約 5 秒的平靜期）。

## 5. 新增專屬 Debug 與對帳機制 (6/3)

* **`debug_kills.py` 腳本**：這是一個獨立的測試工具。在遊戲打完後，直接點兩下這個檔案，它會連上 War Thunder 的 API，把這場遊戲中所有屬於你的原始擊殺日誌全部印出來。可以用來驗證究竟是追蹤器漏抓，還是遊戲 API 根本就沒回傳。
* **`counted_kills.log` 日誌**：從現在開始，只要追蹤器「成功把一筆擊殺加進計數中」，就會把該擊殺的 ID 與時間寫進 `logs/counted_kills.log`。如果覺得擊殺數怪怪的，可以直接拿這個檔案跟遊戲裡的截圖逐一對帳，一目了然！

---
現在你可以安心使用了！不論是網路不穩、遊戲卡頓，甚至是打到一半才開程式，修復後的 Fallback 備援系統都會在背景默默幫你把漏掉的擊殺全部補齊。
